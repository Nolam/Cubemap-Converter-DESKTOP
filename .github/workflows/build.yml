name: Build Electron App

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Get version
        id: get_version
        run: echo "version=$(node -p "require('./package.json').version.replace(/\./g, '-')")" >> $env:GITHUB_OUTPUT

      - name: Install dependencies
        run: npm ci

      - name: Generate icons
        run: npx tsx script/gen-icons.ts

      - name: Build app
        run: npm run build

      - name: Build Electron (Windows)
        run: npx electron-builder --win --config electron-builder.yml
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload Windows artifact
        uses: actions/upload-artifact@v4
        with:
          name: cubemapconverter_${{ steps.get_version.outputs.version }}_windows-portable
          path: release/*.exe

      - name: Publish to GitHub Release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: release/*.exe
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  build-mac:
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Get version
        id: get_version
        run: echo "version=$(node -p "require('./package.json').version.replace(/\./g, '-')")" >> $GITHUB_OUTPUT

      - name: Install dependencies
        run: npm ci

      - name: Generate icons
        run: npx tsx script/gen-icons.ts

      - name: Build app
        run: npm run build

      - name: Build Electron (macOS)
        run: npx electron-builder --mac --config electron-builder.yml
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload macOS artifact
        uses: actions/upload-artifact@v4
        with:
          name: cubemapconverter_${{ steps.get_version.outputs.version }}_macos-portable
          path: release/*.zip

      - name: Publish to GitHub Release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: release/*.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
